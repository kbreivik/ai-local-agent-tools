mcp_server/tools/vm.py                   — service_placement + docker logs (v2.19.0, v2.19.1)
gui/src/components/ServiceCards.jsx      — entity ID fix (qemu→vm) + ⌘ → EntityDrawer + optimistic maint (v2.25.1)
```
