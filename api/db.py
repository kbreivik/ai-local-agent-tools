# This file is superseded by the api/db/ package (api/db/__init__.py).
# Python resolves packages before same-named modules, so this file is never imported.
# Kept only to avoid confusion — safe to delete.
raise ImportError(
    "api/db.py is a legacy stub. Import from the 'api.db' package (api/db/__init__.py) instead."
)
